
import {User, Briefcase, Mail, Code} from 'lucide-react';
export default function Footer() {
  return (
  <>
     <div className="fixed bottom-0 w-full h-24 border-t border-[#2EA566]/20">
      <User size={28} />    
     </div>
  {/* Fix Padding of the Body and make sure the width is full relative to the body */}
  </>
  );
}
